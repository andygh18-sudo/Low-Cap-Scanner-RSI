name: Crypto Scanner

on:
  workflow_dispatch:
  schedule:
    # Four daily scans, deliberately not on the top of the hour to reduce scheduling congestion.
    #- cron: '7 6,12,18,23 * * *'
  
  - cron: '7 00 * * *'
    timezone: 'Europe/London'
  - cron: '7 1 * * *'
    timezone: 'Europe/London'
  - cron: '7 2 * * *'
    timezone: 'Europe/London'
  - cron: '7 3 * * *'
    timezone: 'Europe/London'
  - cron: '7 4 * * *'
    timezone: 'Europe/London'
  - cron: '7 5 * * *'
    timezone: 'Europe/London'
  - cron: '7 6 * * *'
    timezone: 'Europe/London'
  - cron: '7 7 * * *'
    timezone: 'Europe/London'
  - cron: '7 8 * * *'
    timezone: 'Europe/London'
  - cron: '7 9 * * *'
    timezone: 'Europe/London'
  - cron: '7 10 * * *'
    timezone: 'Europe/London'
  - cron: '7 11 * * *'
    timezone: 'Europe/London'
  - cron: '7 12 * * *'
    timezone: 'Europe/London'
  - cron: '7 13 * * *'
    timezone: 'Europe/London'
  - cron: '7 14 * * *'
    timezone: 'Europe/London'
  - cron: '7 15 * * *'
    timezone: 'Europe/London'
  - cron: '7 16 * * *'
    timezone: 'Europe/London'
  - cron: '7 17 * * *'
    timezone: 'Europe/London'
  - cron: '7 18 * * *'
    timezone: 'Europe/London'
  - cron: '7 19 * * *'
    timezone: 'Europe/London'
  - cron: '7 20 * * *'
    timezone: 'Europe/London'
  - cron: '7 21 * * *'
    timezone: 'Europe/London'
  - cron: '7 22 * * *'
    timezone: 'Europe/London'
  - cron: '7 23 * * *'
    timezone: 'Europe/London'

permissions:
  contents: write

jobs:
  scan:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scanner
        run: python scan_engine.py

      - name: Detect new alerts
        run: python alert_signals.py

      - name: Send Top 10 + new signals to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python alert_telegram.py

      - name: Commit latest results
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/latest_scan.json data/latest_scan.csv data/alert_state.json data/new_alerts.json
          git diff --cached --quiet || git commit -m 'Update crypto scan results'
          git push
